# Common aws commands

```
# get current aws user
aws iam get-user 

# works better with federated: Returns details about the IAM user or role whose credentials are used to call the operation. 
aws sts get-caller-identity


```